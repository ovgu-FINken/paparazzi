<!DOCTYPE flight_plan SYSTEM "../flight_plan.dtd">

<!-- Define simple flightplan for manual radio control
This just initializes the absolute basics required for valid compilation.
The actual flightplan stays in the same block forever.

-->
<flight_plan name="RadioControl" lat0="43 33 50.83" lon0="1 28 52.61" ground_alt="147" security_height="0.001" alt="150" max_dist_from_home="150" >
  
  <header>
    #include "autopilot.h"
  </header>
  
    <!-- Define only home way point, as we do not move-->	
  <waypoints>
    <waypoint name="HOME" x="0.0" y="0.0"/>
  </waypoints>
    
  <blocks>

    <!-- Just stay here forever -->
    <block name="Wait Forever">
      <while cond="true"/>
    </block>
  

  </blocks>

  
</flight_plan>
